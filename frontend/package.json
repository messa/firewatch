{
  "name": "firewatch-frontend",
  "version": "0.0.1",
  "description": "React/Next.js frontend for the Firewatch monitoring tool",
  "repository": "github:messa/firewatch",
  "scripts": {
    "dev": "next",
    "export": "rm -rf out && next build && next export",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "next": "^10.2.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "swr": "^0.5.6"
  }
}
